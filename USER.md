# User Document

The full user and use-case document is maintained in [`USERS.md`](USERS.md).

This file exists because the project requirements mention both `USER.md` and `USERS.md` in different places.

